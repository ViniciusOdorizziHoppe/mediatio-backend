const express=require("express");const router=express.Router();const multer=require("multer");const{auth}=require("../../../shared/middlewares/auth.middleware");const morphService=require("./morph.service");const upload=multer({storage:multer.memoryStorage(),limits:{fileSize:10485760,files:12},fileFilter:(r,f,cb)=>{["image/jpeg","image/jpg","image/png","image/webp"].includes(f.mimetype)?cb(null,true):cb(new Error("Formato invalido"))}});router.post("/transform",auth,upload.array("images",12),async(req,res)=>{try{if(!req.files||!req.files.length)return res.status(400).json({success:false,error:"Envie imagens"});const{style,strength,prompt}=req.body;const r=await morphService.batchTransform(req.files,{style:style||"automotive",strength:strength?parseFloat(strength):0.6,prompt});res.json(r)}catch(e){res.status(500).json({success:false,error:e.message})}});module.exports=router;